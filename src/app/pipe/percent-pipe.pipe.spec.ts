import { PercentPipePipe } from './percent-pipe.pipe';

describe('PercentPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PercentPipePipe();
    expect(pipe).toBeTruthy();
  });
});
