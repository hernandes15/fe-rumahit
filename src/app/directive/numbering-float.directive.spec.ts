import { NumberingFloatDirective } from './numbering-float.directive';

describe('NumberingFloatDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberingFloatDirective();
    expect(directive).toBeTruthy();
  });
});
