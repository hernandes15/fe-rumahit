import { PercentDirectiveDirective } from './percent-directive.directive';

describe('PercentDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PercentDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
