import { NumberingIntDirective } from './numbering-int.directive';

describe('NumberingIntDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberingIntDirective();
    expect(directive).toBeTruthy();
  });
});
